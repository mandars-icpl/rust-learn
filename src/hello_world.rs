pub mod hello_world {
    pub fn hello_world() {

        // This is a single line comment which tells that below is the code for hello-world in rust :)
        println!("Hello, world!");

        /*Thank god I don't have any one who requires this multiline comment while talking.
        I mean I have some of them, but I am glad that I don't have to face them regularly. */

        let x = 5 + /* 90 + */5;
        print!("Is `x` 10 or 100? x= {} ", x);
    }
}